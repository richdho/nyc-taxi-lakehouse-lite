# Copy to .env for local overrides
LAKEHOUSE_PROFILE=local
LAKEHOUSE_ROOT=./lake
# Do not set DAGSTER_HOME here — Dagster requires an absolute path, and uv loads
# .env before dagster starts. Use .\make.ps1 dagster (sets it automatically).
